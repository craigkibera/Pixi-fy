[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[requires]
python_version = "3.10"  # Specify the Python version you want to use

[packages]
flask = "*"  # You can specify a version or leave it as "*" to get the latest version of Flask
flask-restful = "*"
flask-sqlalchemy = "*"
flask-bcrypt = "*"
sqlalchemy-serializer = "*"
flask-migrate = "*"

[dev-packages]
